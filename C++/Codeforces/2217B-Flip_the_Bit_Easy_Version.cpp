#include <iostream>
#include <vector>

using namespace std;

int main() {
    int test_cases;
    cin >> test_cases;

    for (int i = 0; i < test_cases; i++) {
        int length, num_special_indices;
        cin >> length >> num_special_indices;

        vector<int> binary (length);
        for (int j = 0; j < length; j++) {
            cin >> binary[j];
        }

        int special_indices;
        cin >> special_indices;
    }

    
}